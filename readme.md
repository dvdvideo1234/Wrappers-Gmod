# Wrappers-Gmod

### Description
This contains all my wrappers for Garry's mod. This can be added as a submodule
for every tool written. It is updated for every repo being connecred.

### Why is it better
Generally because I am lazy. This way the wrapper will get updated for every
addon that uses it and. Otherwise I have to update each addon individually.
